[project]
name = "speculative-decoding"
version = "0.1.0"
description = "Speculative Decoding Research"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.10.0",
    "transformers>=5.3.0",
]
